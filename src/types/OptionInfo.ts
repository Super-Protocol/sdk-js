// Order of keys and type conversion functions for this object in blockchain contract
export const OptionInfoStructure = {
    bandwidth: Number,
    traffic: Number,
    externalPort: Number,
};
export type OptionInfo = {
    bandwidth: number;
    traffic: number;
    externalPort: number;
};
