// Order of keys and type conversion functions for this object in blockchain contract
export const DepositInfoStructure = {
    amount: Number,
    totalLocked: Number,
    profit: Number,
};
export type DepositInfo = {
    amount: string;
    totalLocked: string;
    profit: string;
};
